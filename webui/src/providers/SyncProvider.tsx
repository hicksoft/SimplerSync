import React from "react";

// import React, { useState, createContext } from "react";

// const Context = createContext({ state: Array<Sync>, actions: any });

// interface ProviderProps {
//   children: JSX.Element;
// }

// function SyncProvider({ children }: ProviderProps) {
//   const [syncs, setSyncs] = useState<Array<Sync>>([]);
//   const value = {
//     state: syncs,
//     actions: { create }
//   };
//   return <Context.Provider value={value}>{children}</Context.Provider>;
// }

// interface Sync {
//   name: string;
//   description: string;
//   pattern: Pattern;
// }
